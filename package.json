{
  "name": "quiz_app_mern_electron",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "AliMcdad",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run backend\" \"npm run frontend\" \"npm run electron\"",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm start",
    "electron": "wait-on http://localhost:3000 && cd electron && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../electron && npm install",
    "build": "cd frontend && npm run build",
    "package": "npm run build && cd electron && npm run package"
  },
  "dependencies": {
    "cors": "^2.8.5"
  }
}
